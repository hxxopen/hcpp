# HCPP 版本策略

本文档定义 HCPP 协议、规范文档、Schema 与 SDK 的版本管理规则。

---

## 1. 版本层级

HCPP 使用三层版本：

| 层级 | 示例 | 说明 |
|------|------|------|
| **协议版本** | `0.1`、`0.2`、`1.0` | 写入 Provenance Record 的 `"hcpp"` 字段，决定记录结构与验证逻辑 |
| **规范文档版本** | `SPEC-0.1.0` | 协议文档本身的版本 |
| **SDK / 实现版本** | `go-sdk/v0.1.0` | 与协议版本解耦，可独立快速迭代 |

---

## 2. 协议版本（`"hcpp"` 字段）

每条 Provenance Record **必须**包含协议版本字段：

```json
{
  "hcpp": "0.1",
  ...
}
```

验证器根据此字段选择对应的解析与校验规则，并应支持多版本并行验证。

### 语义化版本规则

协议版本遵循简化的语义化版本（Major.Minor）：

- **主版本（Major）**：`0.x` → `1.0` → `2.0`
  - 允许破坏性变更（字段删除、语义改变、强制算法更换等）
  - 升级主版本时，旧版本记录仍须可被新验证器正确识别（至少能报告版本不兼容）

- **次版本（Minor）**：`0.1` → `0.2` → `0.3`
  - 只允许**新增**可选字段、新增关系类型、新增推荐算法
  - 旧版本记录必须能被新版本验证器完整、正确处理
  - 不得删除或改变已有字段的语义

- **修订版本**：仅用于规范文档的澄清、示例修正、非功能性文字修改，不体现在 `"hcpp"` 字段中。

### 当前版本

| 协议版本 | 状态 | 说明 |
|----------|------|------|
| `0.1` | Draft / Experimental | 当前开发版本，规范可能调整 |

---

## 3. 兼容性承诺

### v0.x 阶段（Experimental）

- 规范仍可能发生不兼容调整。
- 任何破坏性变更都会在 `CHANGELOG.md` 中明确记录。
- 鼓励尽早提供反馈，但生产系统需自行评估风险。

### v1.0 及以后

- 主版本内保持向后兼容。
- 废弃（deprecated）的字段或算法会保留至少一个次版本的过渡期。
- 测试向量与官方验证器作为兼容性基准。

---

## 4. 规范文档与 Schema 版本

- 协议正文位于 `protocol/` 目录，是唯一权威来源。
- JSON Schema 位于 `schema/` 目录，版本与协议版本对齐。
- 文档修订通过 `CHANGELOG.md` 追踪。

推荐在文档头部标注：

```markdown
**协议版本**：0.1  
**文档版本**：SPEC-0.1.0  
**状态**：Draft
```

---

## 5. SDK 版本

SDK 使用独立的语义化版本（SemVer）：

- `MAJOR`：不兼容的 API 变更
- `MINOR`：向后兼容的功能新增
- `PATCH`：向后兼容的问题修复

SDK 版本**不必**与协议版本数字保持一致，但每个 SDK 版本应明确声明其支持的协议版本范围。

示例：

```text
go-sdk/v0.3.1  supports HCPP protocol 0.1 and 0.2
```

---

## 6. 测试向量（Test Vectors）

`test-vectors/` 目录中的官方测试向量是互操作性的重要基准。

- 每个协议次版本都应提供对应的测试向量。
- 实现方应通过官方测试向量验证兼容性。
- 测试向量的变更需谨慎，并记录在 `CHANGELOG.md`。

---

## 7. 废弃策略

当需要废弃某字段、算法或行为时：

1. 在规范中标记为 **Deprecated**，并说明替代方案。
2. 至少保持一个次版本的过渡期（v1.0 之后更严格执行）。
3. 验证器对已废弃特性应给出明确警告，而非直接失败（过渡期内）。
4. 正式移除只能在新的主版本中进行。

---

## 8. 版本发布流程（建议）

1. 在 `protocol/` 中完成规范变更。
2. 更新对应 JSON Schema。
3. 补充或更新测试向量。
4. 更新 `CHANGELOG.md`。
5. 更新 `VERSIONING.md`（如策略有变）。
6. 打 Git Tag（例如 `protocol-0.2`、`v0.2.0`）。
7. 同步更新 SDK 的支持声明。

---

## 9. 与现有记录的关系

- 已按照旧版本协议签发的记录**永远有效**。
- 新验证器必须能正确处理历史版本记录。
- 协议升级不得使历史签名或 Hash 失效。

---

## 10. 变更日志

所有版本变更详见 [CHANGELOG.md](CHANGELOG.md)。

---

**维护原则**：

> 协议的稳定性比功能的速度更重要。宁可慢一点发布 v1.0，也不要在早期频繁破坏已有实现。
